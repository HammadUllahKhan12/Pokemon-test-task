type Pokemon {
  id: ID!
  name: String!
  image: String!
}

type MyPokemon {
  name: String
  url: String
}

type Query {
  party: [Pokemon]
  search(search: String): [Pokemon]
  getAllPokemons(search: String): [MyPokemon]
}
